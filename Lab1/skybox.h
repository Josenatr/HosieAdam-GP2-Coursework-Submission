#pragma once
#include <glm\glm.hpp>
#include <GL\glew.h>
#include "MainGame.h"
/*
class Skybox
{
	enum {
		SKY_LEFT = 0, SKY_BACK, SKY_RIGHT, SKY_FRONT, SKY_TOP, SKY_BOTTOM
	};


public:
	unsigned int side[6];
	float size;
	Skybox();
	Skybox(unsigned int left, unsigned int back, unsigned int right, unsigned int front, unsigned int top, unsigned int bottom, float s);
	~Skybox();
	void drawSkybox();

	MainGame mainGame;

};*/
